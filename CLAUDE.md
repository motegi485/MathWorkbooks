# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

情報系の大学生・大学院進学をめざす人向けの独学用数学問題集シリーズ（線形代数／微分積分・最適化／確率・統計、全3分野・全48章・416問）。**純粋な静的HTMLサイトで、ビルド工程は一切ない。** `index.html` をブラウザで直接開けばそのまま動作する。

## リポジトリ構成

```
index.html                          トップページ（3分野へのリンクカードを描画）
linear-algebra/
  workbook.html                     問題集（全11章103問）
  solutions.html                    解答解説集
calculus-optimization/
  workbook.html                     問題集（全19章159問＋付録「主要公式集」）
  solutions.html
probability-statistics/
  workbook.html                     問題集（全18章154問＋付録「数表」）
  solutions.html
```

各HTMLファイルは完全に自己完結（`<style>` を内部に持つ）しており、外部依存は数式表示用の **MathJax** のみ。本体は `cdnjs.cloudflare.com` の `tex-mml-chtml.min.js` だが、MathJax 4 はフォントを別パッケージとして読むため、実行時には `cdn.jsdelivr.net`（`@mathjax/mathjax-tex-font`）からも取得する。両方に `preconnect` を張ってある。CSP を導入する場合は2つのオリジンを許可すること。ファイル間で共有されるCSS/JSファイルは存在しない ―― `index.html` と各 `workbook.html`/`solutions.html` はそれぞれ独立してスタイルを内包しているため、デザインを一括変更する際は該当する全ファイルの `<style>` ブロックを個別に編集する必要がある。

## `.gitignore` の方針（重要）

`.gitignore` は**許可リスト方式**（`*` で全拒否した上で個別に `!` で解除）になっている。新しいファイル・ディレクトリを追加しても、この許可リストに明示的に加えない限り Git に追跡されない。新規ファイルを作成したら `git status` で追跡対象になっているか必ず確認すること。

## コンテンツの構造規約

- 各分野は「問題集」（`workbook.html`）と「解答解説集」（`solutions.html`）の2冊組で、**問題番号が完全に対応**している（例: 問 3.4 の解説は 解 3.4）。
- 問題側の `id` は `q<章番号>-<問題番号>`（例 `id="q1-1"`）、解答側は `a<章番号>-<問題番号>`（例 `id="a1-1"`）。序章（`ch0`）には問題がなく、末尾に付録がある分野は `id="appendix"` を使う。
- 2冊の間には、表紙とフッタの `p.booknav`（`収録タイトル一覧` / もう一方の冊を別タブで開く / `#top` / `#toc`）というページ単位の導線だけがある。**問題ごとのアンカー参照は張られていない**（問 3.4 から 解 3.4 へ直接飛ぶリンクはない）。番号の対応はテキスト上の規約であり機械的なリンクではないため、問題を追加・削除・並べ替えるときは両ファイルの番号ズレに特に注意する。
- 難易度は3段階（`lv1`＝★基礎／`lv2`＝★★標準／`lv3`＝★★★応用）で、各問題集冒頭・`index.html` の統計表示（`BOOKS` 配列の `levels` 配列）と実際の問題数が一致している必要がある。
- 解答解説集の各問は `方針 → 解答 → ポイント`（`h4` / `h4.alt`）という三段構成が基本形。
- 数式は MathJax の `\( ... \)`（インライン）／`\[ ... \]`（ディスプレイ）記法。HTML内にそのまま埋め込まれているため、バックスラッシュのエスケープ崩れに注意（特に `index.html` 内のJSテンプレートリテラル部分は該当しないが、`workbook.html`/`solutions.html` は生HTMLなのでそのまま `\(...\)` を書く）。
- **数式の中の不等号 `<` は必ず `&lt;` と書くこと。** 生の `<` を書くと、直後が英字の場合（`\(0<p<1\)` の `<p` など）ブラウザがHTMLタグの開始と解釈し、次の `>` までを丸ごと飲み込んで**その問題文が画面から消える**。`>` は生のままでもよい（HTMLのテキストとして妥当）が、`&` は `&amp;` にする（`\begin{pmatrix}2 &amp; 1\end{pmatrix}`）。
- 表は `<div class="tscroll">` で包む。本文幅に収まらない表がページ全体を横スクロールさせる（スマートフォンで本文が読めなくなる）のを防ぐため。`section.prob`/`section.sol`/各囲みにも同じ目的で `overflow-x:auto` を入れてある。
- 本文は `<main>` で囲む。`header.book`（`id="top"`）と `nav.toc`（`id="toc"`）はその外側。

## `index.html` への分野追加

`index.html` 末尾の `<script>` 内 `BOOKS` 配列にオブジェクトを1つ追加するだけでトップページにカードが増える（`id` がそのままディレクトリ名になる）。対応する `<id>/workbook.html` と `<id>/solutions.html` を先に配置しておくこと。`levels` 配列や `chapters`/`problems` はカード表示の統計値であり、実際のHTML内容と手動で一致させる必要がある（自動検証は行われない）。

## 実行時に挿し込む画面専用UI（相互リンク・進捗・しおり）

各冊の末尾にある1つのスクリプトが、問題の見出し `.phead` の右端に操作欄 `.pctl` を作り、そこへ「もう一方の冊への行き先」「解いた印」「しおり」を実行時に挿し込む。**問題側のHTMLには手を入れていない**ので、問題を増減しても追随する。サーバもビルドも不要な、純粋なクライアント側の機能。

- スクリプトは2段構成。前半（相互リンクと `aria-labelledby`）は `localStorage` を使わないので、保存が使えない環境（プライベートモード等）でも動く。後半（進捗としおり）は書き込みプローブに失敗した時点で return する。**この順序を崩さないこと。**
- 冊の識別子は `<body data-book="linear-algebra/workbook">`。これが無いとスクリプトは何もしない。
- CSS も JS も、共有ファイルを持たない方針にあわせて**7ファイルに同一の内容を複製してある**。手を入れるときは全ファイルを揃えること（`.pctl` / `.xref` / `.chk` / `.bmk` / `.bmkbar` / `.bmkhint` / `.progbar` / `nav.toc .prog` / `section.is-marked` / `section.is-done` / `.skip` / `index.html` 側の `.resume` と `.prog-card`）。
- 画面専用のUIなので、`@media print` と `tools/paged.css` の**両方**で隠す必要がある（Paged.js は画面メディアで組版するため `@media print` が効かない）。`build_pdf.py` はJSを有効にしたヘッドレスChromeで刷るため、ここが漏れると紙面に焼き付く。

### 問題 ⇄ 解答の相互リンク

- `id` の規約（`q<章>-<番号>` / `a<章>-<番号>`）を利用し、`sec.id.slice(1)` で取り出した番号にもう一方の接頭辞を付けて行き先を作る。番号の対応はテキスト上の規約でしかないので、**片方だけ番号を変えると404にはならず「無言でページ先頭に着地」する**（存在しないアンカーはブラウザが黙って無視する）。
- 行き先は `target="mathworkbooks-<分野>-<相手の冊>"` の名前付きタブで、押すたびにタブが増えないようにしてある。**`rel="noopener"` を付けてはいけない。** noopener を指定すると、ブラウザは仕様上タブの名前を無視して毎回新しいタブを開く（実機で確認済み）。行き先は同一オリジンの姉妹編なので、`window.opener` が渡っても困らない。

### 進捗（解いた問題）

- 保存キーは `mathworkbooks.progress.v1`。値は `{"<分野>": {"<章>-<番号>": 記録時刻}}` で、**冊ではなく分野単位**。問 3.4 と 解 3.4 は同じ1問なので、どちらの冊でチェックしても同じ状態を指す。
- 表示は3箇所（各問題の `section.is-done`、目次の `nav.toc .prog`、表紙の `.progbar`）。`index.html` も同じキーを読み、カードに `.prog-card` を出す。
- 章ごとの問題数は目次の文字列ではなく実際の `id` から数える（解答解説集の目次は「解 1.1〜1.10」形式で、問題数が書かれていないため）。
- リセットはボタンの2度押しにしてある。`confirm()` はページの組版を止めるので使わない。

### しおり（前回の続き）

- 保存キーは `mathworkbooks.bookmark.v1` の1つだけで、6冊分をまとめて持つ。値は `{"<分野>/<workbook|solutions>": {id, label, tag, t}}`。`index.html` が同じキーを読んでカードに「続きから」を出す。
- 1冊につき1つだけ（別の問題に挟むと前のしおりは外れる）。
- **どちらの保存キーも、名前とデータ形式を変えるときは7ファイルすべてを同時に直すこと。**
- MathJax の組版中は本文が縦に伸び続けるため、`#q3-4` 付きで開いた直後の位置は必ずずれる。組版完了（`#mjx-status` が消える）を `MutationObserver` で待って合わせ直している。同じ理由で移動は即時スクロールにしてある（なめらかスクロールは目的地が動いて途中で止まる）。相互リンクで新しいタブに飛んだときも、この仕組みがそのまま効く。

## 動作確認

自動テストは存在しない。変更後は対象のHTMLファイルをブラウザで直接開いて次を確認する。
- MathJax の数式が正しくレンダリングされるか（生の `\(` が本文に残っていないか。残っていれば上記の `<` の混入を疑う）
- 問題番号と解答番号の対応が崩れていないか
- 幅375px前後で `document.documentElement.scrollWidth` が `clientWidth` を超えていないか（横スクロールの発生）
- `@media print` の印刷レイアウト。ブラウザは既定で背景色を印刷しないため、印刷時は難易度バッジと `方針/解答/ポイント` を「枠線＋濃色文字」に切り替えている。CSSを触ったら、印刷プレビューで両方が読めることを確認する
- 同梱の `*.pdf` は印刷用CSSから生成したもの。印刷まわりのCSSや本文を変更したら PDF も作り直す（`tools/build_pdf.py`。Git 追跡外）
- しおりを触ったら、挟む→リロードで残る→「続きから開く」で戻る→「はずす」で消える、を実機で確認する。`index.html` のカードに「続きから」が出ることも見る
- 相互リンクを触ったら、「解説 →」で姉妹編の同じ番号に着地するか、**2問目以降でタブが増えないか**（名前付きタブの使い回し）、逆方向の「問題 →」も同じか、を実機で確認する
- 進捗を触ったら、チェック→目次と表紙の数が動く→開いたままの姉妹編のタブにも即反映される→リロードで残る→`index.html` のカードに出る→「リセット」2度押しで消える、を確認する
- `localStorage` を使えなくした状態（プライベートウィンドウ等）で開き、進捗としおりのUIは出ないが**相互リンクは動く**ことを確認する
